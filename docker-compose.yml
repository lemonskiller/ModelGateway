services:
  model-gateway:
    build: .
    container_name: model-gateway
    network_mode: host
    ipc: host
    shm_size: 16gb
    privileged: true
    devices:
      - /dev/nvidiactl:/dev/nvidiactl
      - /dev/nvidia-uvm:/dev/nvidia-uvm
      - /dev/nvidia-uvm-tools:/dev/nvidia-uvm-tools
      - /dev/nvidia0:/dev/nvidia0
      - /dev/nvidia1:/dev/nvidia1
      - /dev/nvidia2:/dev/nvidia2
      - /dev/nvidia3:/dev/nvidia3
    environment:
      MODEL_GATEWAY_CONFIG: /app/config/models.yaml
      MODEL_GATEWAY_API_KEY: ${MODEL_GATEWAY_API_KEY:-internal-service-key}
      MODEL_GATEWAY_ADMIN_KEY: ${MODEL_GATEWAY_ADMIN_KEY:-admin-key}
      VLLM_BIN: ${VLLM_BIN:-/tmp/vllm-qwen3-lite-env/bin/vllm}
      VLLM_CUDA_HOME: ${VLLM_CUDA_HOME:-/usr/local/cuda}
    volumes:
      - ./config/models.yaml:/app/config/models.yaml:ro
      - /nfs:/nfs:ro
      - /home/wuxinze/.cache/huggingface:/home/wuxinze/.cache/huggingface:ro
      - /tmp/vllm-qwen3-lite-env:/tmp/vllm-qwen3-lite-env:ro
      - /home/wuxinze/unlimited-ocr-local/.conda:/home/wuxinze/unlimited-ocr-local/.conda:ro
      - /usr/local/cuda:/usr/local/cuda:ro
      - /usr/lib/x86_64-linux-gnu/libcuda.so:/usr/local/nvidia/lib64/libcuda.so:ro
      - /usr/lib/x86_64-linux-gnu/libcuda.so.1:/usr/local/nvidia/lib64/libcuda.so.1:ro
      - /usr/lib/x86_64-linux-gnu/libnvidia-ml.so:/usr/local/nvidia/lib64/libnvidia-ml.so:ro
      - /usr/lib/x86_64-linux-gnu/libnvidia-ml.so.1:/usr/local/nvidia/lib64/libnvidia-ml.so.1:ro
    restart: unless-stopped
